import { useState } from "react";
import "../App.css";

type Props = {
	tip: string;
};

function Tooltip({ tip }: Props) {

	return (
		<div className="Tooltip">
			
		</div>
	);
}

export default Tooltip;
